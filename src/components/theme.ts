import { createTheme } from '@mui/material'

const DEFAULT_THEME = createTheme({
    palette: {
        primary: {
            main: '#1b4193',
        },
        secondary: {
            main: '#b4702e',
        },
    },
})

export default DEFAULT_THEME
