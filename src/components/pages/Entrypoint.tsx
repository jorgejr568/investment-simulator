import { Container, Typography } from '@mui/material'
import { AppBar } from '@components/organisms'

export const EntrypointPage = () => {
    return (
        <>
            <AppBar />

            <Container>
                <Typography variant="h1">Entrypoint</Typography>
            </Container>
        </>
    )
}
